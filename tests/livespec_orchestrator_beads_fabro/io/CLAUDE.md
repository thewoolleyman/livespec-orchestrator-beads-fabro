# tests/livespec_orchestrator_beads_fabro/io/

Tests for the package-local terminal I/O surface under
`.claude-plugin/scripts/livespec_orchestrator_beads_fabro/io/`.

Keep tests focused on preserving observable stdout/stderr behavior while
command modules route stream writes through this seam.
