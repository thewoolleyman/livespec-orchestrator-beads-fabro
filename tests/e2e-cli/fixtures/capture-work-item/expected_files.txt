# No project-root file assertion: the freeform-filing skill creates an
# `origin: freeform`, `gap_id: null` issue in the per-repo beads tenant DB
# (the substrate has no on-disk work-items.jsonl). Success is the exit-0
# round-trip; the absent file list means no per-skill file assertion
# (contract req 4).
