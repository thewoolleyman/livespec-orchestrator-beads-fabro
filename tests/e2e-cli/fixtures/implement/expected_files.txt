# No project-root file assertion: implement records the work-item
# resolution in place on the issue in the per-repo beads tenant DB (close +
# resolution label + AuditRecord into metadata; no on-disk work-items.jsonl).
# Success is the exit-0 round-trip; the absent file list means no per-skill
# file assertion (contract req 4).
