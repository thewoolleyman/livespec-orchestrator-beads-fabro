/livespec-impl-beads:groom Regroom the harness smoke `needs-regroom` item in the beads tenant store: draft a layered decomposition read-only, then on approval file the approved `ready` factory slices with their dependency edges linked and transition the original item out of needs-regroom. Accept the drafted cut and route any spec-change slice to /livespec:propose-change.
