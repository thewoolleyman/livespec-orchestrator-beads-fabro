/livespec-orchestrator-beads-fabro:groom Regroom the harness smoke `backlog` item in the beads tenant store: draft a layered decomposition read-only, then on approval file the approved factory slices through intake routing with their dependency edges linked and explicitly close the original backlog item. Accept the drafted cut and route any spec-change slice to /livespec:propose-change.
