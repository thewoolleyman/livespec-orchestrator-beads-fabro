# No project-root file assertion: the memo is created as a `kind:memo`
# issue in the per-repo beads tenant DB (the substrate has no on-disk
# memos.jsonl). Success is the exit-0 round-trip; the absent file list
# means no per-skill file assertion (contract req 4).
