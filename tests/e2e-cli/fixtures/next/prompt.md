/livespec-impl-beads:next --json Rank the most-ripe impl-side action from the beads tenant store and emit it as JSON. Pure function of store state; no LLM in the ranking path; never mutate the store.
