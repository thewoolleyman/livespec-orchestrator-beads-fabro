# O4 run_turn attributes — receiver allowlist confirmation

Created by an autonomous factory run to confirm that the `run_turn` span's command / config_name / visit / stop_reason / node_id attributes survive the receiver's fail-closed allowlist and reach Honeycomb.
