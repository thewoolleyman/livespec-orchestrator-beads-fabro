# Ledger epic anchor: `bd-ib-zlpeyg`

Plan: idle-factory-visibility.

This file is the overseer-read ledger-epic anchor (resolution path 1 in
`_registry_epic.epic_from_plan_anchor`), so the overseer resolves this track to
its plan epic without depending on a live ledger query. The authoritative plan
state — handoffs, the scope event, and the typed `next_action` — lives on the
beads epic `bd-ib-zlpeyg` (metadata `plan_slug: idle-factory-visibility`,
`spec_id: plan:idle-factory-visibility`); read it there.
