# constraints.md

The default acceptance tier is hermetic and requires no network, credentials,
external services, or host-level mutable state.

The live Beads/Fabro tier is invoked separately through the existing
containerized dispatcher proof targets and requires the documented operator
secret environment.
