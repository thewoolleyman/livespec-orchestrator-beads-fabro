---
topic: grooming-and-slice-size-calibration-realization
author: claude-opus-4-8
created_at: 2026-06-19T07:13:28Z
---

## Proposal: Grooming and slice-size calibration realization

### Target specification files

- SPECIFICATION/contracts.md
- SPECIFICATION/scenarios.md

### Summary

Graduate the concrete realization of the work-grooming design — the maintainer's pre-dispatch work-decomposition layer — into this orchestrator's own SPECIFICATION/. This realizes, for the reference Beads/Dolt + Fabro orchestrator, the repo-agnostic grooming pattern/guidance that livespec core states in its non-functional-requirements.md (alongside the existing core guidance on Orchestrator-internal Dispatcher behavior). A new contracts.md H2 section 'Grooming and slice-size calibration' codifies the four maintainer touchpoints (capture/intake, groom, dispatch, calibration), the augmented-versus-new skill inventory, the Dispatcher's grooming-related behavior (refuse to auto-dispatch human-gated items; on non-convergence mark needs-regroom and surface rather than infinite-retry; emit calibration telemetry; compose next's ranking rather than re-rank inline), the calibration journal fields and the single Fabro workflow-DOT tweak, and the resolved hard-versus-advisory gating split by gate type. Two named open choices are surfaced as explicit unresolved questions for the revise pass. A new scenarios.md narrative (Scenario 7) walks the regroom flow. This is grooming guidance's realization in the orchestrator that owns it; core gains only the repo-agnostic guidance, never a skill or invariant.

### Motivation

The work-grooming design — how a maintainer breaks and sizes work into agent-feedable slices BEFORE autonomous dispatch — was the largest open unknown the user flagged in this ecosystem. It is now drafted in livespec's research/dark-factory-operability/work-breakdown.md (the slice cut-line, the grooming ritual, slice-size calibration, and a Realization & UX section). Per that document's reframe, grooming is Orchestrator-internal: the repo-agnostic pattern is non-functional core guidance, but its CONCRETE realization — a groom front-end, the augmented capture skills, the needs-regroom ledger state, the calibration journal fields, and the Fabro DOT tweak — belongs in the reference orchestrator's OWN spec. livespec-impl-beads is that reference Beads/Dolt + Fabro orchestrator (the family's dogfood default). Codifying the realization here turns the four maintainer touchpoints and the restraint budget (few new artifacts) into a load-bearing architectural commitment this plugin honors, distinct from the always-autonomous Dispatcher path the spec already describes. The companion core non-functional-requirements grooming-guidance change is a SEPARATE propose-change against livespec core; this proposal realizes that guidance and references it as its parent pattern.

### Proposed Changes

Add a new H2 section `## Grooming and slice-size calibration` to `SPECIFICATION/contracts.md`, placed AFTER `## Store-write consent discipline` and BEFORE `## Beads connection model`. The section opens by stating that this realizes the repo-agnostic grooming pattern/guidance that `livespec`'s `non-functional-requirements.md` carries as Orchestrator-internal guidance (beside its existing §"Orchestrator-internal Dispatcher guidance"); core gains only the guidance, never a skill, CLI, or doctor invariant. Grooming operates on this plugin's ledger (the beads tenant DB), is Orchestrator-internal, and is therefore NOT part of `livespec`'s functional cross-boundary contract. The section then states, as `###` subsections:

(A) `### The four maintainer touchpoints` — (1) Capture / intake (augmented): work is filed as today via `capture-work-item` / `capture-impl-gaps`, but each now runs an intake Definition-of-Ready checklist in-dialogue, auto-answering what it can and prompting the human only on the rest, and tags the resulting item `ready` / `needs-regroom` / `not-yet-actionable`. The Definition-of-Ready holds when ALL of these hold, otherwise the item is ROUTED not filed-as-ready: exactly one coherent "done" (one named scenario, scenario-verified; OR the standing gates `just check` + `/livespec:doctor` fully define done with no scenario, gate-verified) — can't name exactly one means it is an epic and routes to `needs-regroom`; the acceptance is autonomously verifiable with no human judgement call; an autonomy tier is assigned (spec-change is human-gated and routes to `/livespec:propose-change` / `/livespec:revise`, everything else is factory-dispatchable); dependencies are linked as beads `blocked-by` edges (ready requires blockers closed AND an acceptance — never deps alone); the repo target is named (one slice maps to one ledger); and the slice is above the size floor (anti-over-split; the floor is human judgement until slice-size calibration yields a value). (2) Groom (the one new maintainer surface): for a `needs-regroom` item the maintainer runs a groom front-end (provisional command name `groom <id>`) — a read-only scoping conversation that DRAFTS a layered decomposition, each candidate slice pre-filled with acceptance / autonomy tier / dependency links / repo target / scope; the maintainer edits and approves (or sends it back to re-draft); on approval the front-end files the slices via the existing `capture-work-item` machinery with dependency edges linked; spec-change slices route to `/livespec:propose-change` rather than the factory. The draft is read-only until the human approves (it proposes; it files nothing until approval). (3) Dispatch (unattended, exceptions only): the Dispatcher drains `ready` slices into Fabro sandboxes by dependency layer, gates each on `just check` + `/livespec:doctor`, merges, and closes — pulling the maintainer in ONLY to SURFACE a `human-gated` (spec-change) item or a `needs-regroom` bounce (escalate-don't-drop, back to touchpoint 2). (4) Calibration (mostly invisible): a periodic analysis pass correlates run outcomes against mechanical size proxies and proposes ceiling thresholds; once a maintainer adopts them they make the intake size-gate flag oversized items advisorily.

(B) `### Skills — augmented versus new`. Augmented (existing skills of this plugin's ten-skill surface): `capture-work-item` and `capture-impl-gaps` run the intake Definition-of-Ready checklist and apply the readiness tag; `process-memos` routes impl-bound memos through the same Definition-of-Ready. New (exactly ONE): the groom front-end (provisional `groom`), the agent-drafts / human-approves regroom surface. NOT skills (Orchestrator machinery): the Dispatcher's grooming-related behavior, the single new `needs-regroom` ledger state, and the calibration analysis pass. State the restraint budget explicitly: the realization adds at most one new front-end + one new ledger state + outcome/size FIELDS on the existing Dispatcher journal + one periodic analysis pass; everything else reuses Beads (ready / dependency layers / labels) and the existing capture front-ends. If the realization ever grew past roughly one new front-end + one new state, that is the signal to stop and reconsider.

(C) `### Dispatcher grooming behavior`. The Dispatcher MUST refuse to auto-dispatch a `human-gated` (spec-change) item — it surfaces it for the maintainer instead. On factory NON-CONVERGENCE (a dispatched slice that will not converge through the janitor gate) the Dispatcher MUST mark the item `needs-regroom` and SURFACE it (escalate-don't-drop), never infinite-retry — non-convergence is the empirical "too big" signal that routes back to the groom front-end. The Dispatcher MUST emit calibration telemetry: an outcome signal plus mechanical size proxies recorded on the EXISTING Dispatcher journal (the journal → Honeycomb leg already designed in the operability preconditions), with NO new always-on service. And — per in-flight work-item `livespec-impl-beads-i3jiny` — the Dispatcher MUST COMPOSE `next`'s ranking rather than re-rank inline: `next` is the single ranking authority and the Dispatcher composes it (the existing fix is the mechanism; this records the spec intent). This subsection is consistent with, and does not relax, the existing `### Machine-path exemption — the Dispatcher` carve-out under §"Store-write consent discipline" (the Dispatcher still only dispositions already-filed items and creates no net-new work-items on its own initiative; marking `needs-regroom` and surfacing is a disposition/escalation of an already-filed item, not a net-new creation).

(D) `### Calibration telemetry and the single Fabro tweak`. Breakdown is entirely UPSTREAM of Fabro (ledger + Dispatcher + skills); Fabro assumes the human has already decomposed work into agent-feedable tasks. So the realization requires NO Fabro platform or setup change. Only two Fabro-adjacent touchpoints: (1) Dispatcher-side run-outcome capture — the Dispatcher already reads Fabro run state and writes the journal, so calibration just records, on that journal, the outcome signal (converged?; fix-loop count; outcome class; wall-clock and token/cost; bounced-to-regroom?) plus the candidate mechanical size proxies (acceptance count; merged-PR diff size; dependency fan-out; spec surface touched; dispatch context size; archetype; repo); and (2) ONE Fabro workflow-DOT tweak within the existing DOT vocabulary — a fix-loop cap plus a "non-converged" exit edge that routes back to the Dispatcher (→ `needs-regroom`), reusing Fabro's existing verify→fix-loop nodes and `max_node_visits` governor. Per-slice sandboxing (a fresh Fabro sandbox per work-item) is already how the Dispatcher uses Fabro and is unchanged. The calibration analysis pass is a periodic query + correlation over the journal, not an always-on service; thresholds it proposes stay provisional and advisory until a maintainer adopts them.

(E) `### Gate type determines hard versus advisory`. Resolve the hard-versus-advisory question by gate TYPE. The STRUCTURAL Definition-of-Ready gates — exactly one coherent "done"; the acceptance exists and is autonomously verifiable; dependencies are linked — are HARD. The SIZE gate (above the floor / below the calibrated ceiling) is ADVISORY because it is data-derived and uncertain; the cut-line's qualitative "one coherent done" remains the primary rule and the calibrated numbers are a secondary, advisory safety net. The reactive ceiling (bail after N fix-loops → route to `needs-regroom`) needs no calibration and is the non-convergence trigger above; the predictive intake size-flag needs calibration and the reactive bail-out is its training signal.

(F) `### Open realization choices`. State, as explicit UNRESOLVED questions to be settled by a maintainer (NOT resolved by this proposal): whether the groom front-end is its OWN skill (the recommended shape) or an "epic mode" of `capture-work-item`; and the exact `needs-regroom` ledger representation (a beads label versus a status). Both choices affect the skill inventory and the ledger-state realization and are deliberately left open here.

Also add a new H2 scenario `## Scenario 7 — Regroom an oversized work-item` to `SPECIFICATION/scenarios.md`, after `## Scenario 6 — Cross-repo Layer 3 loop driver (livespec-resident)`, narrating the regroom flow end to end: (1) an item arrives at `needs-regroom` — either an intake Definition-of-Ready failure (an epic with more than one coherent "done") or a Dispatcher non-convergence bounce; (2) the maintainer runs the groom front-end (`groom <id>`); it reads the item, the relevant spec / scenarios, and the ledger, and DRAFTS candidate slices read-only, each pre-filled with acceptance / autonomy tier / dependency links / repo target / scope, arranged into dependency layers; (3) the maintainer edits the cut / acceptance / deps / tiers and approves (or sends it back to re-draft) — the maintainer OWNS the cut and the acceptance, the front-end only drafts; (4) on approval the front-end files the approved slices via `capture-work-item` with dependency edges linked, and routes any spec-change slice to `/livespec:propose-change` instead of the factory; (5) the Dispatcher then drains the resulting `ready` slices by dependency layer, and after each layer converges re-runs `just check` + `/livespec:doctor` + the named scenarios before the next layer dispatches. State the cross-reference that the grooming PATTERN this scenario realizes is repo-agnostic non-functional guidance in `livespec`'s `non-functional-requirements.md`; the realization shown here (the groom front-end, the `needs-regroom` state, the per-slice fields, the calibration journal fields) is this orchestrator's own.

HEADING-COVERAGE CO-EDIT (carry out at revise time): this proposal adds two new `## ` (H2) headings — `## Grooming and slice-size calibration` in `contracts.md` and `## Scenario 7 — Regroom an oversized work-item` in `scenarios.md`. The revise pass that accepts this proposal MUST add a matching entry for each to `tests/heading-coverage.json` via the same `resulting_files[]` mechanism (path spelled `../tests/heading-coverage.json` because `--spec-target` is the main `SPECIFICATION/` tree), each carrying `"test": "TODO"` with a reason noting it was added by this grooming-realization revise pass and that a real test id is populated by the governed propose-change/revise loop once the heading gains an exercising test (the Scenario 7 entry's reason additionally notes the scenario heading binds to an integration / consumer-tier e2e-cli test per the heading taxonomy's pyramid-tier requirement, never a unit-tier test, mirroring the other Scenario entries). No `## ` heading is added, renamed, or removed elsewhere, so no other heading-coverage entry changes.
