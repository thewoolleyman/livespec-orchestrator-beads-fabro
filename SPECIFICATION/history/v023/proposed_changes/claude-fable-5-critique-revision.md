---
proposal: claude-fable-5-critique.md
decision: modify
revised_at: 2026-07-02T17:24:22Z
author_human: thewoolleyman <chad@thewoolleyman.com>
author_llm: claude-fable-5
---

## Decision and Rationale

All ten consistency findings are ratified: the skill-surface enumeration is consolidated count-free; the retired priority field is swept from every normative surface; the lapsed lifecycle-vocabulary bridge is consolidated (needs-regroom/human-gated/not-yet-actionable re-expressed as the backlog bounce, the admission-valve hold, and the blocked:needs-human routing); the machine-path consent exemption is widened to the five lifecycle dispositions with --no-close-on-merge re-specified as disabling the post-merge disposition writes entirely (matching the shipped dispatcher); the done-vs-closed layering is labeled per clause with --filter=done documented and closed as the beads-layer alias; rank direction is standardized as top-ranked = lexicographically earliest; the console valve surface is specified as orchestrate run approve/accept/reject action ids (Scenario 31); the dead beads-schema-mapping citation is retired in favor of the self-authoritative mapping section; and the implement path is located on the state machine (direct close-in-place, acceptance valve Dispatcher-path-only) with Scenario 1 re-expressed.

## Modifications

Finding 5 (approval-evidence-ambiguity) is resolved by adopting the valve-hold model the S3 implementation ships rather than the proposal's option (a): the 'approval == ready membership / permission settled upstream' framing is DROPPED; an effective-manual ready item is held at the admission valve and surfaced (independent of capacity), and the human's explicit admission through the operator valve surface IS the approval act, with no persisted approval-evidence field. Scenario 23's contradictory Given is re-expressed accordingly. Finding 6 keeps closed as a documented beads-layer alias of --filter=done rather than dropping it. Finding 8's surface is realized as orchestrate run action ids (approve:<id>, accept:<id>, reject:<id>:rework|regroom) with consent classified as the operator's explicit action selection, exercised by new Scenario 31.

## Resulting Changes

- contracts.md
- spec.md
- scenarios.md
- constraints.md
- README.md
